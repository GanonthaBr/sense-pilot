# Known-good stack resolved on Bridges-2 (Python 3.11, CUDA driver 12.6, cu124 wheels).
# torch is installed separately from the cu124 index — see README.
# After any change, regenerate the frozen lockfile on the cluster:
#   pip freeze > requirements.lock.txt

terratorch==1.2.8
torchgeo==0.8.1

# TerraMesh data loading — versions pinned per dataset docs; DO NOT upgrade zarr/numcodecs
webdataset
zarr==2.18.0
numcodecs==0.15.1

# metrics & analysis
numpy
scipy
matplotlib
pyyaml

# terrain (TWI from DEM)
pysheds
